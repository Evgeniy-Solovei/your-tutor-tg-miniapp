# Django
DEBUG=True
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
SECURE_HSTS_SECONDS=3600

# Telegram
TOKEN=
WEB_APP_URL=http://localhost:8000
# Только локально: пропуск HMAC, хедер Telegram-Dev-User / ?dev_tg_id=
TELEGRAM_AUTH_BYPASS=False

# PostgreSQL (обязательно)
POSTGRES_DB=tutor_bot
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
DB_CONN_MAX_AGE=60

# Только для быстрого локального smoke-теста. Полные тесты по умолчанию идут на PostgreSQL.
TEST_USE_SQLITE=False

# bePaid / beGateway
BEPAID_SHOP_ID=
BEPAID_SECRET_KEY=
BEPAID_TEST_MODE=True
BEPAID_CHECKOUT_URL=https://checkout.bepaid.by/ctp/api/checkouts

# === ИИ только для разбора ошибок (не генерация заданий) ===
# Провайдер: deepseek | yandex | gigachat
AI_PROVIDER=deepseek

# --- DeepSeek (рекомендуем: дёшево, OpenAI-совместимый API) ---
# Ключ: https://platform.deepseek.com/
DEEPSEEK_API_KEY=
# AI_MODEL=deepseek-chat
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# --- ЯндексGPT (Yandex Cloud) ---
# YANDEX_API_KEY=
# YANDEX_FOLDER_ID=
# AI_MODEL=yandexgpt-lite
# YANDEX_BASE_URL=https://llm.api.cloud.yandex.net/v1

# --- GigaChat (Сбер) ---
# GIGACHAT_AUTH_KEY=   # base64(client_id:client_secret) или client_id:client_secret
# AI_MODEL=GigaChat
# GIGACHAT_SCOPE=GIGACHAT_API_PERS
